# Step 2: Select Checks

1. Run `select_checks.py`.
2. Inspect unmatched files; add a rule when they represent a repeatable obligation.
3. Read the changed code before accepting path-based classification.
4. Add companion checks for shared contracts, persistence, security boundaries, generated files, and user-facing behavior.

Path rules select work; they do not prove semantic correctness.

Proceed to [Step 3: Verify](03-verify.md).
