# Step 3: Confirm

Ask the user to perform configured authenticated checks when credentials or decrypted data are required.

Report each check as:

- verified automatically;
- verified by the user;
- failed with evidence;
- not tested and why.

Never infer authenticated success from a green deployment job.
